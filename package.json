{
  "name": "ai-home-styler",
  "version": "1.0.0",
  "description": "AI-powered home styling application using OpenAI Images Edit",
  "scripts": {
    "dev": "concurrently \"pnpm --filter frontend dev\" \"pnpm --filter backend dev\"",
    "build": "pnpm --filter frontend build && pnpm --filter backend build",
    "build:frontend": "pnpm --filter frontend build",
    "build:backend": "pnpm --filter backend build",
    "start": "pnpm --filter backend start",
    "install:all": "pnpm install && pnpm --filter frontend install && pnpm --filter backend install",
    "lint": "pnpm --filter frontend lint && pnpm --filter backend lint",
    "type-check": "pnpm --filter frontend type-check && pnpm --filter backend type-check",
    "deploy:prepare": "pnpm build && echo 'Ready for deployment!'",
    "test": "pnpm --filter frontend test && pnpm --filter backend test"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}